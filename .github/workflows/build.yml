name: Build & Release FM Reloaded Mod Manager

on:
  push:
    tags:
      - "v*"     # e.g., v0.5.0
  workflow_dispatch:

jobs:
  pyinstaller-build:
    name: Build (${{ matrix.os }})
    strategy:
      matrix:
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          if [ -f src/requirements.txt ]; then pip install -r src/requirements.txt; fi

      # ---------- WINDOWS ----------
      - name: Build with PyInstaller (Windows)
        if: matrix.os == 'windows-latest'
        shell: cmd
        run: |
          pyinstaller ^
            --clean ^
            --noconfirm ^
            --windowed ^
            --onefile ^
            --name "FM_Reloaded" ^
            --icon src\assets\icon.ico ^
            src\fm26_mod_manager_gui.py

      - name: Package artifact (Windows)
        if: matrix.os == 'windows-latest'
        shell: cmd
        run: |
          mkdir upload
          copy "dist\FM_Reloaded.exe" "upload\FM_Reloaded.exe"

      # ---------- macOS ----------
      - name: Build with PyInstaller (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          pyinstaller \
            --clean \
            --noconfirm \
            --windowed \
            --name "FM_Reloaded" \
            --icon src/assets/icon.icns \
            src/fm26_mod_manager_gui.py

      - name: Package artifact (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          mkdir -p upload
          cd dist
          zip -r "../upload/FM_Reloaded-macOS.zip" "FM_Reloaded.app"
          cd ..

      # ---------- Upload ----------
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os == 'windows-latest' && 'FM_Reloaded-Windows' || 'FM_Reloaded-macOS' }}
          path: upload/*

  release:
    name: Create GitHub Release
    needs: pyinstaller-build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    permissions:
      contents: write
      packages: write
      actions: read

    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: FM_Reloaded-Windows
          path: artifacts/windows
        continue-on-error: true

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: FM_Reloaded-macOS
          path: artifacts/macos
        continue-on-error: true

      - name: List downloaded files
        run: |
          echo "Windows artifacts:"; ls -lah artifacts/windows || true
          echo "macOS artifacts:";   ls -lah artifacts/macos || true

      - name: Create/Update GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/windows/FM_Reloaded.exe
            artifacts/macos/FM_Reloaded-macOS.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
